"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
exports.__esModule = true;
var OpenAI = require('openai-api');
var dotenv = require("dotenv");
dotenv.config();
var file_endings_1 = require("./file-endings");
var file_comment_types_1 = require("./file-comment-types");
var xego_fs_1 = require("./xego-fs");
var OPENAI_API_KEY = process.env.OPENAI_API_KEY;
var openai = new OpenAI(OPENAI_API_KEY);
/*
This code is creating a function that takes in the name of a xego block, the command to build it, and an array of xegos.
The language parameter will be set to Node JS if you are using NodeJS.
- generated by stenography 🤖
*/
var createXegoBlock = function (name, command, xegoPath, language, maxTokens, temperature) {
    if (name === void 0) { name = null; }
    if (xegoPath === void 0) { xegoPath = null; }
    if (language === void 0) { language = 'js'; }
    if (maxTokens === void 0) { maxTokens = 150; }
    if (temperature === void 0) { temperature = 0.1; }
    return __awaiter(void 0, void 0, void 0, function () {
        var engine, prompt_1, xegoTower, topP, presencePenalty, frequencyPenalty, bestOf, n, stream, stop_1, response, err_1;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    _a.trys.push([0, 5, , 6]);
                    engine = 'davinci-codex';
                    language = language.toLowerCase();
                    prompt_1 = '';
                    xegoTower = '';
                    if (!xegoPath) return [3 /*break*/, 2];
                    return [4 /*yield*/, xego_fs_1.readXegoTxtFile(xegoPath)];
                case 1:
                    xegoTower = _a.sent();
                    prompt_1 = "Language: " + language + "\n\n " + xegoTower + "\n\n" + file_comment_types_1["default"][language].start + " " + command + " " + file_comment_types_1["default"][language].end;
                    return [3 /*break*/, 3];
                case 2:
                    prompt_1 = "Language: " + language + "\n\n " + file_comment_types_1["default"][language].start + " " + command + " " + file_comment_types_1["default"][language].end;
                    _a.label = 3;
                case 3:
                    topP = 1;
                    presencePenalty = 0;
                    frequencyPenalty = 0;
                    bestOf = 1;
                    n = 1;
                    stream = false;
                    stop_1 = [file_comment_types_1["default"][language].start];
                    return [4 /*yield*/, openai.complete({ engine: engine, prompt: prompt_1, maxTokens: maxTokens, temperature: temperature, topP: topP, presencePenalty: presencePenalty, frequencyPenalty: frequencyPenalty, bestOf: bestOf, n: n, stream: stream, stop: stop_1 })];
                case 4:
                    response = _a.sent();
                    return [2 /*return*/, {
                            name: name,
                            command: command,
                            xegoTower: xegoTower,
                            code: response.data.choices[0].text.trim(),
                            fileEnding: file_endings_1["default"][language],
                            startComment: file_comment_types_1["default"][language].start,
                            endComment: file_comment_types_1["default"][language].end
                        }];
                case 5:
                    err_1 = _a.sent();
                    throw err_1;
                case 6: return [2 /*return*/];
            }
        });
    });
};
exports["default"] = createXegoBlock;
